#pragma once

#include <cstdint>
#include "math.hpp"

namespace nes{

using namespace gf::math;

using u8 = uint8_t;
using u16 = uint16_t;
using i16 = int16_t;
using u32 = uint32_t;

using i32 = int32_t;

} //namespace nes
